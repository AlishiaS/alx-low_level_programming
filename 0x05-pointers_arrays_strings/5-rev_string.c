#include "main.h"
#include <stdio.h>
/**
 * rev_string - reverses a string
 * rev_string - reverses a string
 * Return: void
 */
void rev_string(char *s)
{
	char ch [] = temp;

	for (int i = strlen(ch) - 1; i>= 0; i--)
	{
		printf("%c, ch[i]");
	}

	return 0;
}
